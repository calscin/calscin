func multiply_number(s.32 a, s.32 b) -> s.32 {
	mut s.32 res = 0;

	for s.32 i => [0..b] {
		res += b;
	}

	return res;
}

func check_number(s.32 test) -> bool {
	if(test <= 53) {
		return true;
	} else if(test == 5) {
		return true;
	} else {
		return false;
	}
}